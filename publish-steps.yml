steps:
  - task: ArchiveFiles@2
    inputs:
      rootFolderOrFile: '$(System.DefaultWorkingDirectory)/build/distributions/game2018-*.zip'
      includeRootFolder: false

  - task: PublishBuildArtifacts@1
